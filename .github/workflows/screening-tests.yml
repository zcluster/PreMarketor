name: Screening validator regression tests
on:
  push:
    branches: [main]
    paths: ['scripts/validate_stock_screening.py', 'tests/test_stock_screening.py', '.github/workflows/screening-tests.yml']
  pull_request:
    paths: ['scripts/validate_stock_screening.py', 'tests/test_stock_screening.py', '.github/workflows/screening-tests.yml']
permissions:
  contents: read
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: python -m unittest discover -s tests -p 'test_stock_screening.py' -v
